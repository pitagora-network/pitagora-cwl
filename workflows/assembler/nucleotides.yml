### available docker containers:
# yyabuki/docker-abyss
# yyabuki/docker-minia
# yyabuki/docker-megahit
# yyabuki/docker-idba
# yyabuki/docker-sga
# yyabuki/docker-spades
# yyabuki/docker-sparse-assembler
# yyabuki/docker-velvet
# yyabuki/docker-velvet-optimiser
docker_name: DOCKERNAME (e.g. yyabuki/docker-idba)
process_tag: TAG (e.g. default)
fq:
  class: File
  path: /path/to/fastq/XXXXX.fastq (paired-end only)
result_directory: /path/to/result
